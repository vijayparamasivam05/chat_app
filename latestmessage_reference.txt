var userReference = FirebaseFirestore.instance.collection('users').doc(id);
      FirebaseFirestore.instance.runTransaction((transaction) async{
        transaction.update(
          userReference,
          {
            'latestMessage': content
          },
        );
      });